service:
  type: ClusterIP

ingress:
  enabled: true
  servicePort: http
  ingressClassName: cloudflare-tunnel
  rules:
    - host: keycloak.supaperman.net
      paths:
        - path: '/*'
          pathType: Prefix

postgresql:
  enabled: false

